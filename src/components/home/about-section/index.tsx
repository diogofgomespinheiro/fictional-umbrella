import React from 'react';

import { IAboutMeContent } from '../../../types';

// Style imports

type AboutSectionProps = {
  content: IAboutMeContent;
};

const AboutSection: React.FC<AboutSectionProps> = ({ content }) => {
  return <div></div>;
};

export default AboutSection;
